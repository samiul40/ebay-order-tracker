def fetch_ad_fees(
    token: str, from_date: str, to_date: str
) -> dict[str, float]:
    """Ad fees require eBay Digital Signatures API (not yet implemented).

    Returns an empty dict — Promo Cost will show 0 in the output.
    """
    return {}
